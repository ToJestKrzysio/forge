services:

  postgres:
    image: postgres:17
    env_file:
      - ./envs/postgres.env
    volumes:
#      - ./data/postgres/:/var/lib/postgresql/data
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"


volumes:
  postgres_data: